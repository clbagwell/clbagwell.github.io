# clbagwell.github.io

Public repository for Github pages site.

The Main app is three boxes in WebGL using THREE.js

Sub-directory A-frame contains example projects written in A-frame

Sub-directory Earth_and_Moon is a rotating Earth and Moon from CodePen

Sub-directory Unity3D_UI is an example UI developed in Unity

Sub-directory VR_Ball_Room is a VR example
